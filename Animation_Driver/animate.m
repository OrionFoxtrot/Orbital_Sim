function flag = animate(series_of_coordinates)
    %Animation Unit (Depreciated)
    %   Takes set's of [[x0,y0,z0;x1,y1,z1],[x00,y00,z00;x01,y01,z01]]
    %   and plots them row by row. Surf not included.

    for i = 1:height(series_of_coordinates)
        while j < width(series_of_coordinates)

        end
    end

    

    flag = 0;
end